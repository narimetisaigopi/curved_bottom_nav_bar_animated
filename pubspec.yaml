name: curved_bottom_nav_bar
description: A customizable curved bottom navigation bar with an animated floating bubble that highlights the active tab.
version: 0.1.0
repository: https://github.com/narimetisaigopi/curved_bottom_nav_bar
issue_tracker: https://github.com/narimetisaigopi/curved_bottom_nav_bar/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
