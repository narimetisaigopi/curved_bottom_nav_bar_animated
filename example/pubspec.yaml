name: curved_bottom_nav_bar_example
description: Example app for the curved_bottom_nav_bar_animated package.
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  curved_bottom_nav_bar_animated:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
